[package]
name = "ex_compute_headless"
version = "0.1.0"
edition = "2024"

[dependencies]
bytemuck = "1.23.2"
encase = { version = "0.11.2", features = ["glam"] }
image = { version = "0.25.6", features = ["png"], default-features = false }
wgpu-engine = { version = "0.1.0", path = "../.." }
